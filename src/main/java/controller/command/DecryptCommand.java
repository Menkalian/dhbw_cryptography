package controller.command;

import network.EnterpriseNetwork;

public class DecryptCommand implements ICommand {
    public DecryptCommand(String query) {
        
    }

    @Override
    public String execute(EnterpriseNetwork network) {
        return "";
    }
}
